// // Run when the page fully loads
//   window.addEventListener('load', () => {
//     // Scroll instantly to the top
//     window.scrollTo(0, 0);

//     // Optional: for smooth scrolling instead of instant jump
//     // window.scrollTo({ top: 0, behavior: 'smooth' });
//   });